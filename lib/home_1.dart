import 'package:flutter/material.dart';

class homeone extends StatelessWidget {
  const homeone({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        elevation: 0,
      ),
      body: Center(
        child: Text('baseer arman'),
      ),
    );
  }
}
